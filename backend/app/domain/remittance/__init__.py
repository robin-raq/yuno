"""Remittance comparison workflow domain (Research → Compliance → Analyst).

Phase A ships the shared typed contracts (`types`) and on-disk fixtures only.
Research parsing (U3), the compliance rules engine (U4), the scripted adapter
(U5), and scoring/analyst logic (U7) arrive in later units.
"""
